#!/usr/bin/env

cmd="$@"

find . \
  -mindepth 1 \
  -maxdepth 1 \
  -type d \
  -not \( \
    -name '.git' -o \
    -name '.svn' -o \
    -name '.hg' \
  \) \
  -exec sh -c "cd {}; out=\$(exec $cmd) ; { [ -n \"\$out\" ] && printf \"[ \$(basename {}) ]\n\$out\n\n\"; }" \;
